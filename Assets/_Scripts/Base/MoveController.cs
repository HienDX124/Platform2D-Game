using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// hàm này anh TA có một hàm move có sẵn trong base rồi, mình chỉ kế thừa ra và dùng thôi. Thực ra
// có thể dùng luôn được nhưng mà e nghĩ nên kế thừa rồi dùng thì sẽ hiểu sâu hơn.
// LTA move package

public class MoveController : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
